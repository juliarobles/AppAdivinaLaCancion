<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gestion.informacion.appadivinalacancion">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppAdivinaLaCancion">
        <activity android:name=".InstruccionesActivity"></activity>
        <activity android:name=".HistorialPartidasActivity" />
        <activity android:name=".JugarResultadoPartidaActivity" />
        <activity android:name=".JugarRondaRespuestaActivity" />
        <activity android:name=".JugarRondaSonarActivity" />
        <activity android:name=".EscogerParticipantesActivity" />
        <activity android:name=".EscogerPlaylistActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>